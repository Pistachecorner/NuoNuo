const url=window.NUONUO_STORE_SUPABASE_URL,key=window.NUONUO_STORE_SUPABASE_ANON_KEY,owner=window.NUONUO_STORE_OWNER_ID,sb=supabase.createClient(url,key);let products=[],cats=[],cart=[];const $=s=>document.querySelector(s),money=n=>`RM ${Number(n||0).toFixed(2)}`,esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function render(){ $('#count').textContent=cart.reduce((n,x)=>n+x.qty,0);$('#total').textContent=money(cart.reduce((n,x)=>n+x.qty*x.price,0));$('#items').innerHTML=cart.length?cart.map((x,i)=>`<div class="line"><div><b>${esc(x.name)}</b><br><span class="qty"><button onclick="qty(${i},-1)">−</button>${x.qty}<button onclick="qty(${i},1)">+</button></span></div><b>${money(x.qty*x.price)}</b></div>`).join(''):'<p>Cart is empty.</p>'}
window.qty=(i,d)=>{cart[i].qty+=d;if(cart[i].qty<1)cart.splice(i,1);render()};window.add=(id)=>{let p=products.find(x=>x.id===id),x=cart.find(x=>x.id===id);x?x.qty++:cart.push({id:p.id,name:p.name,price:Number(p.selling_price||0),cost:Number(p.calculated_cost||0),qty:1});render()};
function menu(cat=''){let list=products.filter(p=>!cat||p.category_id===cat);$('#grid').innerHTML=list.map(p=>`<article class="card"><div class="pic" ${p.image_url?`style="background-image:url('${esc(p.image_url)}')"`:''}>${p.image_url?'':'NuoNuo'}</div><div class="body"><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p><div class="row"><b>${money(p.selling_price)}</b><button class="add" onclick="add('${p.id}')">Add</button></div></div></article>`).join('')||'<p>No products available.</p>'}
async function load(){if(!url||url.includes('PASTE_')||!owner||owner.includes('PASTE_'))return $('#loading').textContent='Connect Supabase in config.js to load your real NuoNuo menu.';let [p,c]=await Promise.all([sb.from('products').select('id,name,description,selling_price,calculated_cost,image_url,category_id').eq('user_id',owner).eq('sales_channel','nuonuo').eq('active',true).order('sort_order').order('name'),sb.from('categories').select('id,name').eq('user_id',owner).eq('sales_channel','nuonuo').order('sort_order').order('name')]);if(p.error||c.error)return $('#loading').textContent='Menu connection failed. Check the public storefront RLS policies.';products=p.data||[];cats=c.data||[];$('#loading').remove();$('#cats').innerHTML=[{id:'',name:'All'},...cats].map((c,i)=>`<button class="tab ${i?'':'active'}" onclick="filterCat(this,'${c.id}')">${esc(c.name)}</button>`).join('');menu()};window.filterCat=(b,id)=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');menu(id)};
$('#shopBtn').onclick=()=>{$('#menu').scrollIntoView({behavior:'smooth',block:'start'});};$('#cart').onclick=()=>{$('#drawer').classList.remove('hidden');render()};$('#close').onclick=()=>$('#drawer').classList.add('hidden');$('#checkout').onclick=()=>{if(!cart.length)return;$('#drawer').classList.add('hidden');$('#date').value=new Date().toISOString().slice(0,10);$('#summary').innerHTML=cart.map(x=>`<div class="summary"><span>${esc(x.name)} × ${x.qty}</span><b>${money(x.qty*x.price)}</b></div>`).join('');$('#checkoutTotal').textContent=money(cart.reduce((n,x)=>n+x.qty*x.price,0));$('#modal').classList.remove('hidden')};$('#x').onclick=()=>$('#modal').classList.add('hidden');$('#done').onclick=()=>$('#success').classList.add('hidden');
$('#form').onsubmit=async e=>{e.preventDefault();$('#error').textContent='';$('#place').disabled=true;try{let subtotal=cart.reduce((n,x)=>n+x.qty*x.price,0),name=$('#name').value.trim(),phone=$('#phone').value.trim();let {data:c,error:ce}=await sb.from('customers').insert({user_id:owner,name,phone,email:$('#email').value.trim(),address:$('#address').value.trim(),notes:'Public NuoNuo website order'}).select('id').single();if(ce)throw ce;let {data:o,error:oe}=await sb.from('orders').insert({user_id:owner,customer_id:c.id,order_number:`WEB-${Date.now().toString().slice(-6)}`,sales_channel:'nuonuo',order_type:'pre_order',scheduled_date:$('#date').value,order_date:$('#date').value,status:'pending',subtotal,discount:0,delivery_fee:0,total:subtotal,payment_status:'unpaid',payment_method:$('#payment').value,notes:[`Public website · ${$('#fulfil').value}`,$('#note').value.trim()].filter(Boolean).join(' | ')}).select('id,order_number').single();if(oe)throw oe;for(let x of cart){let {error}=await sb.from('order_items').insert({user_id:owner,order_id:o.id,product_id:x.id,quantity:x.qty,unit_price:x.price,unit_cost:x.cost,addons_total:0,line_total:x.qty*x.price});if(error)throw error}cart=[];render();$('#modal').classList.add('hidden');$('#orderno').textContent=o.order_number;$('#success').classList.remove('hidden')}catch(err){console.error(err);$('#error').textContent=err.message||'Could not place order.'}finally{$('#place').disabled=false}};load();render();
